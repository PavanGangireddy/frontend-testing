import tw, { styled } from 'twin.macro'

export const PublishWorkbooksContainer = styled.div`
   ${tw`
         w-inherit flex flex-col
    `}
`

export const ListViewContainer = styled.div`
   ${tw`
      w-inherit flex flex-col mb-47px
   `};
`
