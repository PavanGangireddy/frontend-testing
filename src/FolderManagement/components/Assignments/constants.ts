const assignments = 'ASSIGNMENTS'
const deadline = 'DEADLINE'
const score = 'SCORE'
const submittedDateAndTime = 'SUBMITTED TIME'
const status = 'STATUS'
const availableOn = 'AVAILABLE ON'
const submittedOn = 'SUBMITTED ON'

export const activeAssignmentsTableHeader = {
   assignments,
   deadline,
   score,
   submittedDateAndTime,
   status
}

export const upcomingAssignmentsTableHeader = {
   assignments,
   availableOn,
   deadline
}

export const completedAssignmentsTableHeader = {
   assignments,
   submittedOn,
   score
}

export const ASSIGNMENTS_DATE_TIME_FORMAT = 'MMMM d, p'
