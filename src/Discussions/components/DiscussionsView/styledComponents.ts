import tw, { styled } from 'twin.macro'

import { RouteContainer } from '../../styledComponents'

export const DiscussionsWrapper = styled(RouteContainer)``

export const PaginationContainer = styled.div`
   ${tw`p-4 mt-auto`}
`
